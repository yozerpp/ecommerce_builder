### E-commerce Builder
In progress. Public for the checkpoint of the graduation project
## How To Run
1. Install Maven
2. Clone the repository
3. Run the following command in the root directory of the project
```mvn package```
4. Run ```mvn spring-boot:run```